apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "utilitR.fullname" . }}-test-connection"
  labels:
    {{- include "utilitR.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "utilitR.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
