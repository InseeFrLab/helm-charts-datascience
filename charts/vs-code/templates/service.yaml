apiVersion: v1
kind: Service
metadata:
  name: {{ include "vs-code.fullname" . }}
  labels:
    {{- include "vs-code.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vs-code.selectorLabels" . | nindent 4 }}
